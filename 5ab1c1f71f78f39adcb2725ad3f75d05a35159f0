skip: all

Originally cherry-picked into essex-hack-suse from 87f31996, then
later reverted by 62a1e387 and then un-reverted by 77cb1045.

However, it was later modified in trunk by c7e52b5e which ditched
some of the debugging, so we can skip this.
